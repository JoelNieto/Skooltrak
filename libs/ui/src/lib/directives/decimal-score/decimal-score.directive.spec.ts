import { DecimalScoreDirective } from './decimal-score.directive';

describe('DecimalScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalScoreDirective();
    expect(directive).toBeTruthy();
  });
});
