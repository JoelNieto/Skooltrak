import { AssignmentDatePipe } from './assignment-date.pipe';

describe('AssignmentDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AssignmentDatePipe();
    expect(pipe).toBeTruthy();
  });
});
