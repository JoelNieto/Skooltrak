import { HideOutsideDirective } from './hide-outside.directive';

describe('HideOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
